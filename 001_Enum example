https://stackoverflow.com/questions/492115/c-sharp-enums-as-function-parameters

public void myFunc(Enum e)
{
    foreach (var name in Enum.GetNames(e.GetTye()))
    {
        Console.WriteLine(name);
    }
}
